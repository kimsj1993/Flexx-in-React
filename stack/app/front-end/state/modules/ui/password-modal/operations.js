import * as actions from './actions';

const updateTextField = actions.updateTextField;
const clearTextField = actions.clearTextField;
const inputError = actions.inputError;
const clearError = actions.clearError;
const showModal = actions.showModal;
const hideModal = actions.hideModal;

export {
	updateTextField,
	clearTextField,
	inputError,
	clearError,
	showModal,
	hideModal
};