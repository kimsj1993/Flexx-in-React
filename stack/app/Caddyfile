localhost:80 {
    gzip
    proxy /api cgi:5000 {
        websocket
        transparent
        without /api
    }
    proxy /socket.io cgi:5000 {
        websocket
        transparent
    }
    proxy / node:3000 {
        transparent
        except /static
    }
}
